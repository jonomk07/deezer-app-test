import { FanFormatterPipe } from './fan-formatter.pipe';

describe('FanFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new FanFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
